// File: cypress/support/e2e.js

// Import commands.js untuk custom commands (jika //ada)
//import './commands'//

// Tambahkan hook global jika dibutuhkan, contoh:
beforeEach(() => {
  // Misalnya: bisa tambahkan clear cookies atau local storage
  // cy.clearCookies();
  // cy.clearLocalStorage();
});
